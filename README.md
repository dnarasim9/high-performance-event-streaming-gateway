# Event Streaming Gateway

High-performance event streaming gateway built with Go using hexagonal architecture.

## Overview

gRPC-based event ingestion gateway with Kafka as the backing message broker. Supports CloudEvents spec, bidirectional streaming, event replay, and schema validation.

## Getting Started

```bash
make build
make run
```
